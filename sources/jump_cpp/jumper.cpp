#include <stdio.h>

int main() {
    //通过此目录下的图片计算两个相邻方块之间的距离
    //最后需要输出的参数，即两个方块之间的距离
    double distance = 0.0;
    
    // ****************************
    // start your code here

    // ****************************

    //将距离保存在文件中
    freopen("output.txt", "a", stdout);
    printf("%lf", distance);
    return 0;
}